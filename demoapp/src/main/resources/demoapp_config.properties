
# The max delay in ms for the compound interest calc. The
# min delay is 0. This delay is applied on each interval 
# for each account.
compoundInterest.delay.ms=50

# Settings for the concurrent compute compound interest
# thread executor.
taskExecutor.core.pool.size=10
taskExecutor.max.pool.size=20
taskExecutor.queue.capacity=10
taskExecutor.thread.name.prefix=PerfDemoThread-
